from setuptools import setup, find_packages

setup(
    name="supass",
    version="0.0",
    url='',
    license='BSD',
    description="",
    author='zhaolin',
    packages=find_packages('src'),
    package_dir={'': 'src'},
    install_requires=['setuptools'],
)
